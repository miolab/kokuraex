<!DOCTYPE html>
<html lang="ja">
  <head>
    <meta charset="utf-8"/>
    <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>kokura.ex Org [Official]</title>
    <link rel="stylesheet" href="<%= Routes.static_path(@conn, "/css/app.css") %>"/>
    <link rel="icon" href="<%= Routes.static_path(@conn, "/favicon.ico") %>" />
    <script defer type="text/javascript" src="<%= Routes.static_path(@conn, "/js/app.js") %>"></script>
  </head>
  <body>
    <header>
      <section class="container">
        <nav role="navigation">
          <ul>
            <li><a href="https://hexdocs.pm/phoenix/overview.html">Get Started</a></li>
            <%= if function_exported?(Routes, :live_dashboard_path, 2) do %>
              <li><%= link "LiveDashboard", to: Routes.live_dashboard_path(@conn, :home) %></li>
            <% end %>
          </ul>
        </nav>
        <a href="<%= Routes.page_path(@conn, :index) %>" class="phx-logo">
          <%= tag :img,
            src: Routes.static_path(@conn, "/images/logoslim.png"),
            alt: "kokura.ex logo",
            width: "40",
            height: "40"
          %>
        </a>
      </section>
    </header>
    <main role="main" class="container">
      <ul class="row" style="list-style: none;">
        <li>
          <%= link "Home", to: Routes.page_path(@conn, :index) %>
          &nbsp;
        </li>
        <li>
          <%= link "About", to: Routes.about_path(@conn, :index) %>
        </li>
        <li>
          <%= link "Events", to: Routes.events_path(@conn, :index) %>
        </li>
      </ul>
      <p class="alert alert-info" role="alert"><%= get_flash(@conn, :info) %></p>
      <p class="alert alert-danger" role="alert"><%= get_flash(@conn, :error) %></p>
      <%= @inner_content %>
    </main>
    <footer>
      - footer sample text -
    </footer>
  </body>
</html>
